[mgr]
show_hidden = true
show_symlink = true

[preview]
max_width = 1200
max_height = 1200

[opener]
default = [
    { run = 'cmd.exe /c start "" "%s"', orphan = true, for = "windows" },
]

[open]
# prepend_rules = [
# 	{ mime = "*", use = "default" },
# ]
append_rules = [
	{ mime = "*", use = "default" },
]
